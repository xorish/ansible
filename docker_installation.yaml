---
- hosts: all
  remote_user: gns3
  become: true
  tasks:
    - name: Download Docker Script
      command: curl -fsSL https://get.docker.com -o get-docker.sh
    - name: Run Docker Script
      command: sh /home/gns3/get-docker.sh
